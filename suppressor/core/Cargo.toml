[package]
name = "suppressor-core"
version = "0.1.0"
edition = "2024"
description = "domain types and pure helpers for the suppressor daemon"
license = "MIT"
publish = false

[dependencies]
anyhow = "1.0"
chrono = { version = "0.4", features = ["clock"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
